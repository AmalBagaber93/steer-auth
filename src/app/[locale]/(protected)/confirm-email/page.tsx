import { Suspense } from 'react';
import ConfirmEmailScreen from '@/src/compontents/screens/steer-screen/confirm-email-screen/confirm-email-screen';

export default function ConfirmEmailPage() {
    return (
        <Suspense>
            <ConfirmEmailScreen />
        </Suspense>
    );
}
